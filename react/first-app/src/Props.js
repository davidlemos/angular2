import React from 'react'

const Props = props => {
    return(
        <div>
            <h1>{props.titulo}</h1>
            <p>{props.text}</p>
        </div>
    )
}

export default Props